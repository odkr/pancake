# This file lists every file that should be included in a release, one file
# per line. Every file listed must be placed in the project's git repository.
# If a filename refers to a directory, that directory is included as a whole.
# Relative paths are interpreted as relative to the root of that repository.
# Empty lines and lines that start with a '#' are ignored.

pancake.lua
README.md
LICENSE.txt
doc
